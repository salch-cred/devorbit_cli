# Example Skill

When asked to summarize a repository, first call `repo_map`, then `build_project_index`, and use `search_project_index` for relevant symbols. Keep the final architecture summary concise.
